using System;

namespace TowerSoft.Utilities.Attributes {
    /// <summary>
    /// Used with AllPropertyComparer to mark a property to comparer. Only used if useCompareFields = true on the AllPropertyComparer
    /// </summary>
    public class CompareFieldAttribute : Attribute { }
}
